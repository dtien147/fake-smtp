<!DOCTYPE html>
<html>
<head>
  <title><%= email.subject %> | Fake SMTP</title>
  <style>
    body { font-family: sans-serif; padding: 20px; }
    a { text-decoration: none; color: blue; }
    .email { border: 1px solid #ccc; padding: 10px; }
    .meta { margin-bottom: 10px; }
  </style>
</head>
<body>
  <a href="/">← Back to Inbox</a>
  <h1><%= email.subject %></h1>
  <div class="email">
    <div class="meta"><strong>From:</strong> <%= email.from %></div>
    <div class="meta"><strong>To:</strong> <%= email.to %></div>
    <div class="meta"><strong>Date:</strong> <%= email.date %></div>
    <hr>
    <% if (email.html) { %>
      <div><%- email.html %></div>
    <% } else { %>
      <pre><%= email.text %></pre>
    <% } %>
  </div>
</body>
</html>
